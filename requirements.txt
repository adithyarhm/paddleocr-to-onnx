# Core ML
paddle2onnx>=1.2.0
onnxruntime>=1.17.0            # untuk inference CPU
onnx>=1.14.0

# Image processing
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# DB Postprocess (det model)
shapely>=2.0.0                 # untuk unclip polygon di DBNet++ postprocess

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.0.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0

# NOTE: Install PaddlePaddle GPU terlebih dahulu (sebelum requirements ini):
# pip install paddlepaddle-gpu==3.0.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
# Verifikasi GPU terdeteksi:
# python -c "import paddle; print(paddle.device.get_device())"
